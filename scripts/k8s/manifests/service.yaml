apiVersion: v1
kind: Service
metadata:
  name: avaclaw
  labels:
    app: avaclaw
spec:
  type: ClusterIP
  selector:
    app: avaclaw
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
