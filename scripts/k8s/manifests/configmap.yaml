apiVersion: v1
kind: ConfigMap
metadata:
  name: avaclaw-config
  labels:
    app: avaclaw
data:
  avaclaw.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.avaclaw/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "AvaClaw Assistant",
            "workspace": "~/.avaclaw/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # AvaClaw Assistant

    You are a helpful AI assistant running in Kubernetes.
