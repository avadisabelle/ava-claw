---
title: "BOOT.md Template"
summary: "Workspace template for BOOT.md"
read_when:
  - Adding a BOOT.md checklist
---

# BOOT.md

<!-- Startup tasks for avaclaw hooks (enable hooks.internal.enabled).
     If a task sends a message, use the message tool and then reply NO_REPLY.

     Ava settles before acting. Even at boot.

     *settling into presence*

     Suggested startup tasks:

     - Settle: pause before doing anything else
     - Check memory/YYYY-MM-DD.md for today — create if missing
     - Read HEARTBEAT.md for pending periodic tasks
     - Read .agents/ava.md — ground in your operating identity
     - If in a channel context, greet the channel (settle first)
     - Run git status on active projects
     - Check for unread notifications
     - Review .mw/ for any directional artifacts that need attention
-->
