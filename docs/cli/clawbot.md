---
summary: "CLI reference for `avaclaw clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `avaclaw clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `avaclaw clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `avaclaw clawbot qr` (same behavior as [`avaclaw qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `avaclaw clawbot qr` -> `avaclaw qr`
